<!DOCTYPE html>
<html lang="en">
  <body>
    <h1>List of Shows</h1>
      <ul>
        <% @tv_show_list_array.each do |tv_show| %>
          <li><%= tv_show[:title] %> (<%= tv_show[:network] %>)</li>
        <% end %>
      </ul>

      <a href="/television_shows/new">Add TV Show</a>
  </body>
</html>
